# Dependencies - monitoring-service

Consume:

- Eventos `schedule.*`, `document.*`, `audit.*` para alimentar dashboards.

Publica:

- `metric.alert` (consumido por `audit-service` y equipos de operaciones).
