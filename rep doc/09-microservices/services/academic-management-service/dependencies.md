# Dependencies - academic-management-service

Consume:

- `reference-data-service` (para validar sedes/modalidades si aplica).

Publica:

- `course.created`, `course.updated` (consumido por `scheduling-service`, `document-service`).
